# docker-compose.override.yml
version: '3.8'

services:
  api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
    volumes:
      - ./src/WebApi:/app
      - /app/bin
      - /app/obj